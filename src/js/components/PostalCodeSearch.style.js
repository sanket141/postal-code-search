import styled from 'styled-components';

const InteractiveWrapper = styled.div`
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    position: absolute;
    background-color: rgba(222, 40, 50, 0.15);
`;

export default InteractiveWrapper;
